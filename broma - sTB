<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>¡Broma Web! 😈</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            text-align: center; 
            padding: 50px; 
            background-color: #f0f0f0; 
            margin: 0;
        }
        button { 
            padding: 10px 20px; 
            font-size: 16px; 
            background: #ff6b6b; 
            color: white; 
            border: none; 
            border-radius: 5px; 
            cursor: pointer; 
        }
        button:hover { background: #ff5252; }
        
        /* Estilos para modal personalizado (mejor que alert nativo) */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.5);
            animation: fadeIn 0.3s;
        }
        .modal-content {
            background-color: #fff;
            margin: 15% auto;
            padding: 20px;
            border-radius: 10px;
            width: 80%;
            max-width: 400px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            animation: bounceIn 0.5s;
        }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        @keyframes bounceIn { 0% { transform: scale(0.3); } 50% { transform: scale(1.05); } 70% { transform: scale(0.9); } 100% { transform: scale(1); } }
        .close-btn {
            background: #4CAF50;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }
        .close-btn:hover { background: #45a049; }
    </style>
</head>
<body>
    <h1>¡Bienvenido a la Broma! 😈</h1>
    <p>¡Ja ja! Intenta cerrar esta página... pero las alertas volverán (solo 5 veces).</p>
    <button onclick="detenerBroma()">Detener la Broma</button>
    
    <!-- Modal personalizado para las "alertas" -->
    <div id="miModal" class="modal">
        <div class="modal-content">
            <h2>¡Ja ja! 😈</h2>
            <p id="mensajeModal">¡Esta broma no para! Intenta cerrarla... pero volverá.</p>
            <button class="close-btn" onclick="cerrarModal()">Cerrar</button>
        </div>
    </div>
    
    <script>
        let bromaActiva = true;
        let intervaloBroma;  // Para controlar el intervalo
        let contador = 0;
        const MAX_ALERTAS = 5;
        
        function mostrarBroma() {
            if (!bromaActiva || contador >= MAX_ALERTAS) return;
            
            // Actualiza el mensaje con el contador
            document.getElementById('mensajeModal').innerHTML = 
                `¡Ja ja! ¡Esta broma no para! 😈<br>Alerta ${contador + 1} de ${MAX_ALERTAS}. Intenta cerrarla... pero volverá.`;
            
            // Muestra el modal
            document.getElementById('miModal').style.display = 'block';
            contador++;
        }
        
        function cerrarModal() {
            document.getElementById('miModal').style.display = 'none';
            
            // Si no se ha detenido y no hemos llegado al límite, programa la siguiente
            if (bromaActiva && contador < MAX_ALERTAS) {
                setTimeout(mostrarBroma, 1000);  // Pausa de 1 segundo
            } else if (contador >= MAX_ALERTAS) {
                mostrarMensajeFinal();
            }
        }
        
        function mostrarMensajeFinal() {
            alert("¡Broma terminada! 😊 Puedes cerrar la página ahora.");  // Usa alert nativo solo para el final
        }
        
        function detenerBroma() {
            bromaActiva = false;
            document.getElementById('miModal').style.display = 'none';  // Cierra modal si está abierto
            alert("¡Broma detenida! Buena jugada. 😉");
        }
        
        // Inicia la broma al cargar la página
        window.onload = function() {
            // Usa setInterval para un control más preciso (cada 2 segundos: 1s pausa + 1s para cerrar modal)
            intervaloBroma = setInterval(function() {
                if (bromaActiva && contador < MAX_ALERTAS) {
                    mostrarBroma();
                } else {
                    clearInterval(intervaloBroma);
                    if (contador >= MAX_ALERTAS) {
                        mostrarMensajeFinal();
                    }
                }
            }, 2000);  // Intervalo total: muestra cada 2 segundos
        };
        
        // Cierra modal si se hace clic fuera de él
        window.onclick = function(event) {
            const modal = document.getElementById('miModal');
            if (event.target == modal) {
                cerrarModal();
            }
        }
    </script>
</body>
</html>

   
